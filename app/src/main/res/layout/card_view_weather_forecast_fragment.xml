<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    app:cardBackgroundColor="@color/white"
    app:cardCornerRadius="10dp"
    android:layout_marginStart="10sp"
    android:layout_marginEnd="10sp"
    app:cardElevation="2dp"
    android:clickable="true"
    android:focusable="true"
    app:cardPreventCornerOverlap="true"
    app:cardUseCompatPadding="true">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Text View to display timestamp forecast -->
        <TextView
            android:id="@+id/card_view_forecast_time"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Time"
            android:textAlignment="center"
            android:textColor="@color/black"
            android:textSize="10sp"
            android:textStyle="bold" />

        <!-- ImageVIew to display weather forecast picture -->
        <ImageView
            android:id="@+id/card_view_img_weather"
            android:layout_width="100sp"
            android:layout_height="100sp"
            android:contentDescription="Picture of forecast weather"
            android:padding="5dp"
            android:src="@drawable/cloudy_3" />

        <!-- Text View to display forecast temperature -->
        <TextView
            android:id="@+id/card_view_forecast_temperature"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAlignment="center"
            android:text="Temperature"
            android:textColor="@color/black"
            android:textSize="14sp"
            android:textStyle="bold" />

    </LinearLayout>

</androidx.cardview.widget.CardView>